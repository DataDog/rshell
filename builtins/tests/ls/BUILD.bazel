load("@rules_go//go:def.bzl", "go_test")

go_test(
    name = "ls_test",
    srcs = ["ls_fuzz_test.go"],
    data = glob(["testdata/**"]),
    deps = [
        "//builtins/testutil",
        "//interp",
    ],
)

load("@bazel_skylib//lib:subpackages.bzl", "subpackages")
load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "strings_cmd",
    srcs = ["strings.go"],
    importpath = "github.com/DataDog/rshell/builtins/strings_cmd",
    visibility = ["//visibility:public"],
    deps = ["//builtins"],
)

go_test(
    name = "strings_cmd_test",
    size = "small",
    srcs = [
        "strings_bench_test.go",
        "strings_cmd_vuln_hunt_test.go",
        "strings_pentest_test.go",
    ],
    deps = [
        "//builtins/testutil",
        "//internal/interpoption",
        "//interp",
        "@cc_mvdan_sh_v3//syntax",
        "@com_github_stretchr_testify//assert",
        "@com_github_stretchr_testify//require",
    ],
)

filegroup(
    name = "srcs",
    srcs = glob(["**"]) + [
        "%s:srcs" % d
        for d in subpackages.all(
            allow_empty = True,
            fully_qualified = True,
        )
    ],
    visibility = ["//visibility:public"],
)

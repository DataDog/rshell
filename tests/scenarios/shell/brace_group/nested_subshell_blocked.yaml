# skip: subshells are intentionally blocked in the restricted shell
skip_assert_against_bash: true
description: Subshell after subshell is blocked.
input:
  script: |+
    (echo hello) && (echo world)
expect:
  stdout: ""
  stderr: |+
    subshells are not supported
  exit_code: 2

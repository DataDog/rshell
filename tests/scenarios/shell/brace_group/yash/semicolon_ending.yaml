# yash: grouping-p.tst - brace grouping ending with semicolon
description: Brace group ending with semicolon before closing brace.
input:
  script: |+
    { echo foo; }
expect:
  stdout: |+
    foo
  stderr: ""
  exit_code: 0

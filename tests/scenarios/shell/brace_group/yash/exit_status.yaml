description: Brace group exit status reflects the last command.
input:
  script: |+
    { true; false; }
    echo $?
    { false; true; }
    echo $?
expect:
  stdout: |+
    1
    0
  stderr: ""
  exit_code: 0

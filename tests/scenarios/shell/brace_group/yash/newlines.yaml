# yash: grouping-p.tst - newlines in brace grouping
description: Brace group with newline-separated commands.
input:
  script: |+
    {
    echo foo
    }
expect:
  stdout: |+
    foo
  stderr: ""
  exit_code: 0

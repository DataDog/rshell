description: Brace group exit terminates the entire script.
input:
  script: |+
    a=1
    { a=2; echo $a; exit; echo not reached; }
    echo not reached
expect:
  stdout: |+
    2
  stderr: ""
  exit_code: 0

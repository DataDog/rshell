description: Multiple process substitutions in one command.
skip_assert_against_bash: true
input:
  script: |+
    cat <(echo first) <(echo second)
expect:
  stdout: "first\nsecond\n"
  stderr: ""
  exit_code: 0
  stdout_windows: ""
  stderr_contains_windows: ["process substitution is not supported on this platform"]

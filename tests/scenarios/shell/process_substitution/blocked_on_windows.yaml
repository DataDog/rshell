description: Process substitution is blocked on Windows (no /dev/fd support).
skip_assert_against_bash: true
input:
  script: |+
    cat <(echo hello)
expect:
  stdout: "hello\n"
  stderr: ""
  exit_code: 0
  stdout_windows: ""
  stderr_contains_windows: ["process substitution is not supported on this platform"]

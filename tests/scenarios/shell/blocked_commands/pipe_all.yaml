test_against_bash: false
description: Pipe all (|&) is not supported (bash extension).
input:
  script: |+
    echo hello |& cat
expect:
  stdout: ""
  stderr: |+
    |& is not supported
  exit_code: 2

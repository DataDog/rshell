# skip: eval/select are intentionally blocked in the restricted shell
skip_assert_against_bash: true
description: Eval command is not available.
input:
  script: |+
    eval echo hello
expect:
  stdout: ""
  stderr: |+
    eval: command not found
  exit_code: 127

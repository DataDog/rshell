skip_assert_against_bash: true
description: Eval builtin is intentionally blocked in the restricted shell.
input:
  script: |+
    eval echo hello
expect:
  stdout: ""
  stderr_contains:
    - "not found"
  exit_code: 127

skip_assert_against_bash: true
description: Export builtin is intentionally blocked in the restricted shell.
input:
  script: |+
    export FOO=bar
expect:
  stdout: ""
  stderr_contains:
    - "not supported"
  exit_code: 2

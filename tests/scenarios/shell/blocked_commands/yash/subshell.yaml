skip_assert_against_bash: true
description: Subshells are intentionally blocked in the restricted shell.
input:
  script: |+
    (echo hello)
expect:
  stdout: ""
  stderr_contains:
    - "subshell"
  exit_code: 2

# skip: case statements are intentionally blocked in the restricted shell
skip_assert_against_bash: true
description: Case with default pattern is blocked.
input:
  script: |+
    case unknown in
      *) echo default;;
    esac
expect:
  stdout: ""
  stderr: "case statements are not supported\n"
  exit_code: 2

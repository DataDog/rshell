# skip: case statements are intentionally blocked in the restricted shell
skip_assert_against_bash: true
description: Case with character class pattern is blocked.
input:
  script: |+
    case b in
      [abc]) echo matched;;
      *) echo no;;
    esac
expect:
  stdout: ""
  stderr: "case statements are not supported\n"
  exit_code: 2

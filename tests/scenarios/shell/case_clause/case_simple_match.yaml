# skip: case statements are intentionally blocked in the restricted shell
skip_assert_against_bash: true
description: Simple case statement with exact match is blocked.
input:
  script: |+
    case hello in
      hello) echo matched;;
    esac
expect:
  stdout: ""
  stderr: |+
    case statements are not supported
  exit_code: 2

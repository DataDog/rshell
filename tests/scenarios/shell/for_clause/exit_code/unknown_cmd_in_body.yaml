description: Unknown command in for loop body produces error on stderr.
input:
  allowed_commands: ["nonexistent_cmd"]
  script: |+
    for i in a; do nonexistent_cmd; done
expect:
  stdout: ""
  stderr_contains:
    - "nonexistent_cmd: command not found"
  exit_code: 127

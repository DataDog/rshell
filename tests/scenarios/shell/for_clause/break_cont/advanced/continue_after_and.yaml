description: Continue after && operator continues the for loop.
input:
  script: |+
    for i in 1 2; do
        true && continue
        echo not reached
    done
    echo done
expect:
  stdout: |+
    done
  stderr: ""
  exit_code: 0

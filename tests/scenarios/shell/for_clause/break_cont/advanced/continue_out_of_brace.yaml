description: Continue inside a brace group still affects the enclosing for loop.
input:
  script: |+
    for i in 1 2; do
        { continue; }
        echo not reached
    done
    echo done
expect:
  stdout: |+
    done
  stderr: ""
  exit_code: 0

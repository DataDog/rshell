# yash: break-p.tst - breaking after ||
description: Break after || operator exits the for loop.
input:
  script: |+
    for i in 1; do
        false || break
        echo not reached
    done
    echo done
expect:
  stdout: |+
    done
  stderr: ""
  exit_code: 0

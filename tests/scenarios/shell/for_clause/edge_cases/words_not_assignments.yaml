description: For loop words are not evaluated as variable assignments.
input:
  script: |+
    for i in a=1 b=2; do echo $i; done
    echo $a $b
expect:
  stdout: |+
    a=1
    b=2

  stderr: ""
  exit_code: 0

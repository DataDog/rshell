description: Heredoc stdin combines with file-target stdout redirect on the same command.
input:
  allowed_paths: ["$DIR"]
  script: |+
    cat <<EOF > greeting.txt
    hello
    world
    EOF
    cat greeting.txt
expect:
  stdout: |+
    hello
    world
  stderr: ""
  exit_code: 0

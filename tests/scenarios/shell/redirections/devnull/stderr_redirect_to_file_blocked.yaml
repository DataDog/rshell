skip_assert_against_bash: true
description: Stderr redirection to a real file (not /dev/null) is still blocked.
input:
  script: |+
    echo hello 2> /tmp/errors.txt
expect:
  stdout: ""
  stderr: |+
    > file redirection is not supported
  exit_code: 2

# skip: redirect restrictions are an rshell-specific security feature
skip_assert_against_bash: true
description: Output redirection outside AllowedPaths is blocked.
input:
  script: |+
    echo hello > /tmp/output.txt
expect:
  stdout: ""
  stderr_contains:
    - "permission denied"
  exit_code: 1

description: Redirect stdout to /dev/null then dup stderr to stdout suppresses all output.
input:
  script: |+
    cat nonexistent_file >/dev/null 2>&1
expect:
  stdout: ""
  stderr: ""
  exit_code: 1

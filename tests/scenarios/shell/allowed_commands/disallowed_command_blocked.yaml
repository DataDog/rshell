skip_assert_against_bash: true
description: A command not listed in allowed_commands is blocked.
input:
  allowed_commands:
    - rshell:echo
  script: |+
    cat /dev/null
expect:
  stdout: ""
  stderr_contains:
    - "cat /dev/null: invocation not permitted by policy"
  exit_code: 127

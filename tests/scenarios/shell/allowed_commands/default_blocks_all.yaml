skip_assert_against_bash: true
description: Empty AllowedCommands list blocks all commands
input:
  allowed_commands: []
  script: |+
    echo hello
expect:
  stdout: ""
  stderr: "echo: command not allowed\n"
  exit_code: 1

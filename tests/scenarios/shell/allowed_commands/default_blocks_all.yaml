skip_assert_against_bash: true
description: Default (no AllowedCommands set) blocks all commands
input:
  allowed_commands: []
  script: |+
    echo hello
expect:
  stdout: ""
  stderr: "echo: command not allowed\n"
  exit_code: 1

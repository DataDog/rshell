skip_assert_against_bash: true
description: A command listed in allowed_commands can be executed.
input:
  allowed_commands:
    - rshell:echo
  script: |+
    echo hello
expect:
  stdout: |+
    hello
  stderr: ""
  exit_code: 0

skip_assert_against_bash: true
description: Command substitution is blocked by the shell itself, not by AllowedCommands
input:
  allowed_commands: ["echo"]
  script: |+
    echo $(cat /dev/null)
expect:
  stdout: ""
  stderr: "command substitution is not supported\n"
  exit_code: 2

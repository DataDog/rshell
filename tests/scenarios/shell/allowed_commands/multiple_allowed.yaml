skip_assert_against_bash: true
description: Multiple allowed commands work
input:
  allowed_commands: ["echo", "true"]
  script: |+
    echo hello
    true
    echo world
expect:
  stdout: "hello\nworld\n"
  stderr: ""
  exit_code: 0

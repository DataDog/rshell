description: IFS set to semicolon splits fields on each delimiter.
input:
  script: |+
    IFS=";"
    A="a;b;c"
    for w in $A; do echo "[$w]"; done
expect:
  stdout: "[a]\n[b]\n[c]\n"
  exit_code: 0

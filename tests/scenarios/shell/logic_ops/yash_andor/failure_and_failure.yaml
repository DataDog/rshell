# yash: andor-p.tst - 2-command list, failure && failure
description: First command fails so second is skipped in && list.
input:
  script: |+
    false && false
expect:
  stdout: ""
  stderr: ""
  exit_code: 1

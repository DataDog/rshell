# yash: andor-p.tst - 2-command list, success || failure
description: First succeeds so second is skipped in || list.
input:
  script: |+
    echo 1 || false
expect:
  stdout: |+
    1
  stderr: ""
  exit_code: 0

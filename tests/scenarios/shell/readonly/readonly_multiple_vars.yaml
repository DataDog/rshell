# Derived from yash readonly-p.tst - readonly multiple variables
skip_assert_against_bash: true
description: Readonly with multiple variables is blocked.
input:
  script: |+
    readonly A=1 B=2 C=3
expect:
  stdout: ""
  stderr: "readonly is not supported\n"
  exit_code: 2

# skip: readonly is intentionally blocked in the restricted shell
description: Readonly inside a subshell is rejected at parse time.
skip_assert_against_bash: true
input:
  script: |+
    (readonly X=1)
expect:
  stdout: ""
  stderr: "readonly is not supported\n"
  exit_code: 2

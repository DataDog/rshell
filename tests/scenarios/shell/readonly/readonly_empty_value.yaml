# skip: readonly is intentionally blocked in the restricted shell
skip_assert_against_bash: true
description: Readonly with empty value assignment is blocked.
input:
  script: |+
    readonly X=
expect:
  stdout: ""
  stderr: "readonly is not supported\n"
  exit_code: 2

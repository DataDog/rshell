# skip: readonly is intentionally blocked in the restricted shell
skip_assert_against_bash: true
description: Readonly with -f flag for functions is blocked.
input:
  script: |+
    readonly -f myFunc
expect:
  stdout: ""
  stderr: "readonly is not supported\n"
  exit_code: 2

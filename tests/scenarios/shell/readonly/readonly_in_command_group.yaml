# skip: readonly is intentionally blocked in the restricted shell
skip_assert_against_bash: true
description: Readonly inside a command group is blocked.
input:
  script: |+
    { readonly X=1; }
expect:
  stdout: ""
  stderr: "readonly is not supported\n"
  exit_code: 2

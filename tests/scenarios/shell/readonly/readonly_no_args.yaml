# Derived from yash readonly-p.tst - readonly without arguments
skip_assert_against_bash: true
description: Readonly without arguments is blocked.
input:
  script: |+
    readonly
expect:
  stdout: ""
  stderr: "readonly is not supported\n"
  exit_code: 2

description: "$? reflects the exit status of the last command"
input:
  script: |+
    true
    echo "$?"
    false
    echo "$?"
    true
    echo "$?"
expect:
  stdout: |
    0
    1
    0
  stderr: ""
  exit_code: 0

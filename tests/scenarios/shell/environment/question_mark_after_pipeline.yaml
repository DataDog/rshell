description: "$? reflects exit status of the last command in a pipeline"
input:
  script: |+
    true | false
    echo "$?"
    false | true
    echo "$?"
expect:
  stdout: |
    1
    0
  stderr: ""
  exit_code: 0

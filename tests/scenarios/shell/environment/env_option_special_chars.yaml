description: "Env option variable with special characters in value"
test_against_bash: false
input:
  interpreter_env:
    SPECIAL: "hello world!@#"
  script: |
    echo "$SPECIAL"
expect:
  stdout: "hello world!@#\n"
  exit_code: 0

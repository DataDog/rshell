description: "Env option can provide empty-string values"
skip_assert_against_bash: true
input:
  interpreter_env:
    EMPTY_VAR: ""
  script: |
    echo "val=[$EMPTY_VAR]"
expect:
  stdout: "val=[]\n"
  exit_code: 0

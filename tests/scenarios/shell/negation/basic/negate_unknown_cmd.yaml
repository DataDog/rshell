description: Negating a command-not-allowed (exit 1) produces exit code 0.
input:
  allowed_commands: []
  script: |+
    ! nonexistent_command_xyz
expect:
  stdout: ""
  stderr_contains:
    - "command not allowed"
  exit_code: 0

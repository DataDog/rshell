skip_assert_against_bash: true
description: The += append operator is not supported.
input:
  script: |+
    X=hello
    X+=world
expect:
  stdout: ""
  stderr: |+
    += is not supported
  exit_code: 2

description: Assigning $? to a variable captures it before the assignment resets $? to 0.
input:
  script: |+
    true | exit 42
    code=$?
    echo "captured=$code"
    echo "current=$?"
expect:
  stdout: |+
    captured=42
    current=0
  stderr: ""
  exit_code: 0

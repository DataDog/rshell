description: Using $? multiple times in the same echo expands correctly.
input:
  script: |+
    false
    echo "$? $?"
    true
    echo "$? $?"
expect:
  stdout: |+
    1 1
    0 0
  stderr: ""
  exit_code: 0

description: The $? variable is 127 after an unknown command.
input:
  allowed_commands: ["echo", "nonexistent_cmd"]
  script: |+
    nonexistent_cmd
    echo $?
expect:
  stdout: |+
    127
  stderr_contains:
    - "command not found"
  exit_code: 0

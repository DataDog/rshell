skip_assert_against_bash: true
description: The $? variable is 1 after an unknown command.
input:
  script: |+
    nonexistent_cmd
    echo $?
expect:
  stdout: |+
    1
  stderr_contains:
    - "command not allowed"
  exit_code: 0

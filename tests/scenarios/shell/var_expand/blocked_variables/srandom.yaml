test_against_bash: false
description: The $SRANDOM variable has no special meaning and expands to empty.
input:
  script: |+
    echo "[$SRANDOM]"
expect:
  stdout: |+
    []
  stderr: ""
  exit_code: 0

test_against_bash: false
description: The $RANDOM variable has no special meaning and expands to empty.
input:
  script: |+
    echo "[$RANDOM]"
expect:
  stdout: |+
    []
  stderr: ""
  exit_code: 0

description: break inside an else-branch exits the enclosing while loop.
input:
  script: |+
    while true; do
      if false; then
        :
      else
        break
      fi
    done
    echo done
expect:
  stdout: |+
    done
  stderr: |+
  exit_code: 0

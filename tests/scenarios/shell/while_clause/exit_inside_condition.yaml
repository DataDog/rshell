description: exit inside the while condition propagates the exit code without running the body.
input:
  script: |+
    while exit 7; do echo body; done
expect:
  stdout: |+
  stderr: |+
  exit_code: 7

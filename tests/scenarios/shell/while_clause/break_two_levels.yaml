description: break 2 exits two nested while loops at once.
input:
  script: |+
    while true; do
      while true; do
        echo inner
        break 2
      done
      echo unreachable
    done
    echo done
expect:
  stdout: |+
    inner
    done
  stderr: ""
  exit_code: 0

# Derived from yash input-p.tst - mixed comments and whitespace
description: Script with comments and whitespace but no commands exits successfully.
input:
  script: |+
    # comment one
      # indented comment
    # comment two
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

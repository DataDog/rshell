description: Explicit fd 0 input redirect (0< file) reads from the file.
setup:
  files:
    - path: input.txt
      content: "explicit fd0\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    cat 0< input.txt
expect:
  stdout: |+
    explicit fd0
  stderr: ""
  exit_code: 0

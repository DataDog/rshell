description: Multiple unknown commands each produce errors, last exit code wins.
input:
  allowed_commands: ["unknown_cmd_1", "unknown_cmd_2"]
  script: |+
    unknown_cmd_1
    unknown_cmd_2
expect:
  stdout: ""
  stderr_contains:
    - "unknown_cmd_1: command not found"
    - "unknown_cmd_2: command not found"
  exit_code: 127

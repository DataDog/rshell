description: Unknown command in a pipeline produces error but pipeline continues.
input:
  script: |+
    echo hello | unknown_filter_cmd
expect:
  stdout: ""
  stderr_contains:
    - "command not found"
  exit_code: 127

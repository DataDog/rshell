description: Assignment values containing glob metacharacters remain literal.
setup:
  files:
    - path: a.txt
      content: "a\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    P=*.txt
    echo "$P"
expect:
  stdout: |+
    *.txt
  stderr: ""
  exit_code: 0

description: Pattern with many consecutive stars behaves like a single star.
setup:
  files:
    - path: a.txt
      content: ""
    - path: b.txt
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    echo *****.txt
expect:
  stdout: |+
    a.txt b.txt
  stderr: ""
  exit_code: 0

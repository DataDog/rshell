description: Question mark does not match slash in path.
setup:
  files:
    - path: foo/dir/file
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    echo foo?dir?file
expect:
  stdout: |+
    foo?dir?file
  stderr: ""
  exit_code: 0

description: Write redirect after valid commands writes the target and execution continues.
input:
  allowed_paths: ["$DIR"]
  script: |+
    echo before
    echo data > output.txt
    echo after
    cat output.txt
expect:
  stdout: |+
    before
    after
    data
  stderr: |+
  exit_code: 0

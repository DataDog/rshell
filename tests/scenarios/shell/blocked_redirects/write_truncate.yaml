description: Output redirection (>) writes inside allowed paths.
input:
  allowed_paths: ["$DIR"]
  script: |+
    echo hello > output.txt
    cat output.txt
expect:
  stdout: |+
    hello
  stderr: |+
  exit_code: 0

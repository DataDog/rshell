skip_assert_against_bash: true
description: Unsupported input fd (3< file) is rejected at validation time.
input:
  script: |+
    echo hello 3< somefile
expect:
  stdout: ""
  stderr: |+
    3< input fd redirection is not supported
  exit_code: 2

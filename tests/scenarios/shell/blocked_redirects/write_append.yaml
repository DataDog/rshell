description: Append redirection (>>) appends inside allowed paths.
setup:
  files:
    - path: output.txt
      content: "old\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    echo hello >> output.txt
    cat output.txt
expect:
  stdout: |+
    old
    hello
  stderr: |+
  exit_code: 0

description: Output redirection with variable filename writes inside allowed paths.
input:
  allowed_paths: ["$DIR"]
  script: |+
    F=out.txt
    echo hello > "$F"
    cat "$F"
expect:
  stdout: |+
    hello
  stderr: |+
  exit_code: 0

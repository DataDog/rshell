description: Pipeline exit status comes from the last command.
input:
  script: |+
    echo a | true
    echo a $?
    echo b | false
    echo b $?
expect:
  stdout: |+
    a 0
    b 1
  stderr: ""
  exit_code: 0

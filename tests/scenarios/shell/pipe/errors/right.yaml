# skip: error message format differs from bash
skip_assert_against_bash: true
description: Unknown command on right side of pipe sets exit code 127.
input:
  script: |+
    echo ok | foo
expect:
  stdout: ""
  stderr_contains: ["foo", "unknown command"]
  exit_code: 127

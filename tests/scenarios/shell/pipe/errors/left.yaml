description: Unknown command on left side of pipe sends error to stderr; right side still runs.
input:
  allowed_commands: ["echo"]
  script: |+
    foo | echo ok
expect:
  stdout: |+
    ok
  stderr: |+
    command not allowed: foo
  exit_code: 0

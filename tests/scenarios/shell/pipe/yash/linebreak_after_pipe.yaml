# yash: pipeline-p.tst - linebreak after |
description: Linebreak after pipe operator continues the pipeline.
input:
  script: |+
    printf '%s\n' foo bar |
    tail -n 1 |
    cat
expect:
  stdout: |+
    bar
  stderr: ""
  exit_code: 0

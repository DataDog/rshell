# Derived from yash function-p.tst - simple function definition
skip_assert_against_bash: true
description: Simple function definition with parentheses is blocked.
input:
  script: |+
    hello() { echo world; }
expect:
  stdout: ""
  stderr: |+
    function declarations are not supported
  exit_code: 2

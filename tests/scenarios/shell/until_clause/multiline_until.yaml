description: Multi-line until loop with break runs the body once and exits.
input:
  script: |+
    until false
    do
      echo loop
      break
    done
expect:
  stdout: |+
    loop
  stderr: |+
  exit_code: 0

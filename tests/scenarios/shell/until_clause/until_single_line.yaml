# Derived from yash until-p.tst - single line until
skip_assert_against_bash: true
description: Single-line until loop is blocked.
input:
  script: |+
    until true; do echo loop; done
expect:
  stdout: ""
  stderr: |+
    while/until loops are not supported
  exit_code: 2

description: exit inside the until condition propagates the exit code without running the body.
input:
  script: |+
    until exit 7; do echo body; done
expect:
  stdout: |+
  stderr: |+
  exit_code: 7

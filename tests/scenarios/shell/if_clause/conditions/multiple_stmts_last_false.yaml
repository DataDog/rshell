description: If condition with multiple statements where last is false.
input:
  script: |+
    if true; false; then echo yes; else echo no; fi
expect:
  stdout: |+
    no
  stderr: ""
  exit_code: 0

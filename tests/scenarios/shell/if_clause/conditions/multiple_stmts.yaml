description: If condition with multiple statements uses last exit code.
input:
  script: |+
    if false; true; then echo yes; else echo no; fi
expect:
  stdout: |+
    yes
  stderr: ""
  exit_code: 0

skip_assert_against_bash: true
description: If statement as part of a pipeline (blocked - subshell needed for pipe).
input:
  script: |+
    echo hello | if true; then cat; fi
expect:
  stdout: |+
    hello
  stderr: ""
  exit_code: 0

# yash: if-p.tst - redirection on if
description: If statement output can be piped.
input:
  script: |+
    if true; then echo hello; fi | cat
expect:
  stdout: |+
    hello
  stderr: ""
  exit_code: 0

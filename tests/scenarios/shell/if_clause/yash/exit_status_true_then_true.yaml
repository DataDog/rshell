# yash: if-p.tst - exit status of if, true condition, true body
description: Exit status of if reflects the body command.
input:
  script: |+
    if true; then true; fi
    echo $?
expect:
  stdout: |+
    0
  stderr: ""
  exit_code: 0

# yash: if-p.tst - execution path of if, true
description: If block executes when condition is true.
input:
  script: |+
    if true; then echo reached; fi
expect:
  stdout: |+
    reached
  stderr: ""
  exit_code: 0

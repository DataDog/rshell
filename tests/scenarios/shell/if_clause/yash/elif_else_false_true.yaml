# yash: if-p.tst - execution path of if-elif-else, false-true
description: Elif branch executes in if-elif-else when if fails and elif succeeds.
input:
  script: |+
    if false; then echo 1; elif true; then echo 2; else echo 3; fi
expect:
  stdout: |+
    2
  stderr: ""
  exit_code: 0

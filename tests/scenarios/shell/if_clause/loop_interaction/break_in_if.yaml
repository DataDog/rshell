description: Break inside if body within a for loop stops the loop.
input:
  script: |+
    for i in a b c; do
      if [ "$i" = "b" ]; then
        break
      fi
      echo $i
    done
expect:
  stdout: |+
    a
  stderr: ""
  exit_code: 0

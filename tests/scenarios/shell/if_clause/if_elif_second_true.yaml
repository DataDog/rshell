description: If-elif takes second elif when earlier conditions fail.
input:
  script: |+
    if false; then echo a; elif false; then echo b; elif true; then echo c; else echo d; fi
expect:
  stdout: |+
    c
  stderr: ""
  exit_code: 0

description: cd accepts a directory operand with a trailing slash.
setup:
  files:
    - path: child/leaf
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    cd child/
    pwd
expect:
  stdout_contains: ["/child"]
  stdout_contains_windows: ["\\child"]
  stderr: ""
  exit_code: 0

description: cd updates the PWD environment variable.
setup:
  files:
    - path: child/leaf
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    cd child
    echo "$PWD"
expect:
  stdout_contains: ["/child"]
  stdout_contains_windows: ["\\child"]
  stderr: ""
  exit_code: 0

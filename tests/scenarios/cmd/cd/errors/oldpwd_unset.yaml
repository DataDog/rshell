description: cd - errors when OLDPWD has not been set yet.
skip_assert_against_bash: true
input:
  allowed_paths: ["$DIR"]
  script: |+
    cd -
expect:
  stdout: ""
  stderr: |+
    cd: OLDPWD not set
  exit_code: 1

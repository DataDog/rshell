description: cd with two operands is rejected with exit 1.
skip_assert_against_bash: true
input:
  allowed_paths: ["$DIR"]
  script: |+
    cd a b
expect:
  stdout: ""
  stderr: |+
    cd: too many arguments
  exit_code: 1

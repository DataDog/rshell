description: cd rejects an unknown short flag with exit 1 and a stderr diagnostic.
skip_assert_against_bash: true
input:
  allowed_paths: ["$DIR"]
  script: |+
    cd -x
expect:
  stdout: ""
  stderr: |+
    cd: invalid option -- 'x'
    Try 'cd --help' for more information.
  exit_code: 1

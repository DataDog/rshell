description: cd with no operand errors when HOME is not set.
skip_assert_against_bash: true
input:
  allowed_paths: ["$DIR"]
  script: |+
    cd
expect:
  stdout: ""
  stderr: |+
    cd: HOME not set
  exit_code: 1

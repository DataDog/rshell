description: cd -LP is parsed as -L followed by -P; last (-P) wins.
setup:
  files:
    - path: real/leaf
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    cd -LP real
    pwd
expect:
  stdout_contains: ["/real"]
  stdout_contains_windows: ["\\real"]
  stderr: ""
  exit_code: 0

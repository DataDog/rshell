# skip: error message format differs from bash
skip_assert_against_bash: true
description: exit with mixed numeric/alpha argument returns error.
input:
  script: |+
    exit 42xyz
expect:
  stdout: ""
  stderr_contains:
    - "numeric argument required"
  exit_code: 2

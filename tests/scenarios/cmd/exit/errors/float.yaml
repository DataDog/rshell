skip_assert_against_bash: true
description: >
  Exit with a floating-point argument is invalid; does not exit the shell.
  skip_assert_against_bash because error message format differs.
input:
  script: |+
    exit 1.5
expect:
  stdout: ""
  stderr: "1.5: numeric argument required\n"
  exit_code: 2

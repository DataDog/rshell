description: ps --help prints usage information and exits with code 0.
skip_assert_against_bash: true
input:
  script: |+
    ps --help
expect:
  stdout_contains:
    - "Usage: ps"
    - "PIDLIST"
  exit_code: 0

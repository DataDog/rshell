description: ps accepts repeated format options and a signed multi-key sort specification.
skip_assert_against_bash: true
input:
  script: |+
    ps -o pid -o comm --sort=-pid,+comm
expect:
  stdout_contains:
    - "PID"
    - "COMMAND"
  exit_code: 0

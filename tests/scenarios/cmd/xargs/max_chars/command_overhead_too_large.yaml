description: xargs fails with exit 1 when -s is smaller than the command name overhead, even on empty input.
input:
  script: |+
    printf '' | xargs -s 1 echo
expect:
  stdout: ""
  stderr: |+
    xargs: cannot fit single argument within argument list size limit
  exit_code: 1

description: xargs aborts with exit 1 when -x is set and a batch would exceed the -s limit.
input:
  script: |+
    printf 'a b c\n' | xargs -s 8 -n 3 -x echo
expect:
  stdout: ""
  stderr: |+
    xargs: argument list too long
  exit_code: 1

description: "xargs -I{} -n 1 keeps replacement mode silently (GNU: -n 1 is compatible with -I, no warning)."
input:
  script: |+
    printf 'foo\nbar\n' | xargs -I{} -n 1 echo X{}Y
expect:
  stdout: |+
    XfooY
    XbarY
  stderr: ""
  exit_code: 0

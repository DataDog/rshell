description: xargs -I processes single and double quotes in input items, stripping them and preserving whitespace inside (matches GNU).
input:
  script: |+
    printf "'a b'\n\"c d\"\n" | xargs -I{} echo X{}X
expect:
  stdout: |+
    Xa bX
    Xc dX
  stderr: |+
  exit_code: 0

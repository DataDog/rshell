description: xargs rejects a single-quoted string that is not closed before EOF (no trailing newline).
input:
  script: |+
    printf "'abc" | xargs echo
expect:
  stdout: ""
  stderr: |+
    xargs: unmatched single quote; by default quotes are special to xargs unless you use the -0 option
  exit_code: 1

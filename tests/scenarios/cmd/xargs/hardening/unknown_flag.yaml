# skip: pflag error message format differs from GNU xargs's error wording.
skip_assert_against_bash: true
description: xargs rejects unknown flags with exit code 1.
input:
  script: |+
    xargs --not-a-flag
expect:
  stdout: ""
  stderr: |+
    xargs: unknown flag: --not-a-flag
  exit_code: 1

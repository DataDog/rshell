description: head -n N larger than file length outputs all lines without error.
setup:
  files:
    - path: file.txt
      content: "alpha\nbeta\ngamma\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    head -n 100 file.txt
expect:
  stdout: "alpha\nbeta\ngamma\n"
  stderr: ""
  exit_code: 0

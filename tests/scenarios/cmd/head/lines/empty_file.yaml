description: head on an empty file produces no output and exits 0.
setup:
  files:
    - path: empty.txt
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    head empty.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

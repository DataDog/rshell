# Derived from GNU coreutils head.pl basic tests
description: head --lines=N long form is equivalent to -n N.
skip_assert_against_bash: true  # --lines= is a GNU extension; BusyBox head (bash:5.2 image) only supports -n
setup:
  files:
    - path: file.txt
      content: "alpha\nbeta\ngamma\ndelta\nepsilon\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    head --lines=3 file.txt
expect:
  stdout: "alpha\nbeta\ngamma\n"
  stderr: ""
  exit_code: 0

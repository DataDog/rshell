description: head exits 1 when -n is given a non-numeric argument.
setup:
  files:
    - path: file.txt
      content: "hello\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    head -n abc file.txt
expect:
  stdout: ""
  stderr_contains: ["head:"]
  exit_code: 1

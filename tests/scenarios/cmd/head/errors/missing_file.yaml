# Derived from standard POSIX error behavior
description: head exits 1 and prints an error when a file does not exist.
input:
  allowed_paths: ["$DIR"]
  script: |+
    head nonexistent.txt
expect:
  stdout: ""
  stderr_contains: ["head:"]
  exit_code: 1

# Per RULES.md: file access must be sandboxed via AllowedPaths
description: head is blocked from reading files outside the allowed paths sandbox.
skip_assert_against_bash: true  # intentional sandbox restriction; bash can read /etc/passwd freely
setup:
  files:
    - path: local.txt
      content: "local\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    head /etc/passwd
expect:
  stdout: ""
  stderr_contains: ["head:"]
  exit_code: 1

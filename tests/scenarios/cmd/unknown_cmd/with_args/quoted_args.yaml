# skip: error message format differs from bash
skip_assert_against_bash: true
description: An unknown command with quoted arguments still prints "unknown command".
input:
  script: |+
    foo "hello world" 'single quoted'
expect:
  stdout: ""
  stderr_contains: ["foo", "unknown command"]
  exit_code: 127

description: An unknown command with quoted arguments still prints "command not allowed".
input:
  allowed_commands: []
  script: |+
    foo "hello world" 'single quoted'
expect:
  stdout: ""
  stderr: |+
    command not allowed: foo
  exit_code: 1

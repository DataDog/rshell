description: An unknown command with arguments still prints "command not allowed" for the command name.
input:
  allowed_commands: []
  script: |+
    foo arg1 arg2 arg3
expect:
  stdout: ""
  stderr: |+
    command not allowed: foo
  exit_code: 1

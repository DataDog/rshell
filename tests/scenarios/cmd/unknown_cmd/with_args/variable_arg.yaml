description: An unknown command with a variable-expanded argument still prints "command not allowed".
input:
  allowed_commands: []
  script: |+
    MY_VAR=hello
    foo $MY_VAR
expect:
  stdout: ""
  stderr: |+
    command not allowed: foo
  exit_code: 1

skip_assert_against_bash: true
description: An unknown command with flag-like arguments still prints "command not allowed".
input:
  script: |+
    foo --verbose -n 5
expect:
  stdout: ""
  stderr: |+
    command not allowed: foo
  exit_code: 1

skip_assert_against_bash: true
description: The sh command is not a builtin and is rejected as not allowed.
input:
  script: |+
    sh -c "echo hello"
expect:
  stdout: ""
  stderr: |+
    command not allowed: sh
  exit_code: 1

skip_assert_against_bash: true
description: The sh command is not a builtin and is rejected as not allowed.
input:
  allowed_commands: []
  script: |+
    sh -c "echo hello"
expect:
  stdout: ""
  stderr: |+
    sh: command not allowed
  exit_code: 1

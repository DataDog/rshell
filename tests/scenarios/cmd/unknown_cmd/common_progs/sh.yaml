skip_assert_against_bash: true
description: The sh command is not a builtin and is rejected as not found.
input:
  allowed_commands: ["sh"]
  script: |+
    sh -c "echo hello"
expect:
  stdout: ""
  stderr_contains: ["sh: command not found"]
  exit_code: 127

skip_assert_against_bash: true
description: The bash command is not a builtin and is rejected as not allowed.
input:
  allowed_commands: []
  script: |+
    bash -c "echo hello"
expect:
  stdout: ""
  stderr: |+
    bash: command not allowed
  exit_code: 1

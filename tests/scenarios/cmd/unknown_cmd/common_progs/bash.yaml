skip_assert_against_bash: true
description: The bash command is not a builtin and is rejected as not found.
input:
  allowed_commands: ["bash"]
  script: |+
    bash -c "echo hello"
expect:
  stdout: ""
  stderr_contains: ["bash: command not found"]
  exit_code: 127

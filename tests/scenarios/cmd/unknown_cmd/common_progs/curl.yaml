skip_assert_against_bash: true
description: The curl command is not a builtin and is rejected as not allowed.
input:
  allowed_commands: []
  script: |+
    curl http://example.com
expect:
  stdout: ""
  stderr: |+
    command not allowed: curl
  exit_code: 1

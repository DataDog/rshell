description: The curl command is not a builtin and is rejected as not found.
input:
  allowed_commands: ["curl"]
  script: |+
    curl http://example.com
expect:
  stdout: ""
  stderr_contains: ["curl: command not found"]
  exit_code: 127

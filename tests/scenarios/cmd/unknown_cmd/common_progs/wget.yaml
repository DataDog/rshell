skip_assert_against_bash: true
description: The wget command is not a builtin and is rejected as not allowed.
input:
  script: |+
    wget http://example.com
expect:
  stdout: ""
  stderr: |+
    command not allowed: wget
  exit_code: 1

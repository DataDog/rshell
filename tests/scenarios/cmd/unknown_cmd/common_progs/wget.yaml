description: The wget command is not a builtin and is rejected as not found.
input:
  allowed_commands: ["wget"]
  script: |+
    wget http://example.com
expect:
  stdout: ""
  stderr_contains: ["wget: command not found"]
  exit_code: 127

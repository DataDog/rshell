# python is now a builtin in rshell; update this test accordingly.
skip_assert_against_bash: true
description: The python command is a builtin that executes Python 3 code.
input:
  script: |+
    python -c "print('hello')"
expect:
  stdout: |+
    hello
  stderr: |+
  exit_code: 0

description: The python command is not a builtin and is rejected as not found.
input:
  allowed_commands: ["python"]
  script: |+
    python -c "print('hello')"
expect:
  stdout: ""
  stderr_contains: ["python: command not found"]
  exit_code: 127

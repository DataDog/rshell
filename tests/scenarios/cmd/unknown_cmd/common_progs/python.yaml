skip_assert_against_bash: true
description: The python command is not a builtin and is rejected as not allowed.
input:
  allowed_commands: []
  script: |+
    python -c "print('hello')"
expect:
  stdout: ""
  stderr: |+
    command not allowed: python
  exit_code: 1

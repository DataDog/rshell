skip_assert_against_bash: true
description: The mv command is not a builtin and is rejected as not found.
input:
  allowed_commands: ["mv"]
  script: |+
    mv old.txt new.txt
expect:
  stdout: ""
  stderr_contains: ["mv: command not found"]
  exit_code: 127

skip_assert_against_bash: true
description: The cp command is not a builtin and is rejected as not allowed.
input:
  allowed_commands: []
  script: |+
    cp source.txt dest.txt
expect:
  stdout: ""
  stderr: |+
    cp: command not allowed
  exit_code: 1

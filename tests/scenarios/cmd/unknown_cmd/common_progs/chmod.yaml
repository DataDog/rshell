skip_assert_against_bash: true
description: The chmod command is not a builtin and is rejected as not found.
input:
  allowed_commands: ["chmod"]
  script: |+
    chmod 755 file.txt
expect:
  stdout: ""
  stderr: "chmod: command not found\n"
  exit_code: 127

skip_assert_against_bash: true
description: The chmod command is not a builtin and is rejected as not allowed.
input:
  allowed_commands: []
  script: |+
    chmod 755 file.txt
expect:
  stdout: ""
  stderr: |+
    command not allowed: chmod
  exit_code: 1

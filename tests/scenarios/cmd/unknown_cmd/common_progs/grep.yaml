skip_assert_against_bash: true
description: The grep command is not a builtin and is rejected as unknown.
input:
  script: |+
    grep pattern file.txt
expect:
  stdout: ""
  stderr: |+
    grep: command not found
  exit_code: 127

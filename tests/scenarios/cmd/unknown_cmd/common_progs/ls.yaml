skip_assert_against_bash: true
description: The ls command is not a builtin and is rejected as unknown.
input:
  script: |+
    ls
expect:
  stdout: ""
  stderr: |+
    ls: command not found
  exit_code: 127

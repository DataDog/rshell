skip_assert_against_bash: true
description: The rm command is not a builtin and is rejected as not allowed.
input:
  script: |+
    rm file.txt
expect:
  stdout: ""
  stderr: |+
    command not allowed: rm
  exit_code: 1

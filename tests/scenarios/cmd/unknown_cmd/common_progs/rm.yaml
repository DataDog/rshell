test_against_bash: false
description: The rm command is not a builtin and is rejected as unknown.
input:
  script: |+
    rm file.txt
expect:
  stdout: ""
  stderr: |+
    rm: command not found
  exit_code: 127

skip_assert_against_bash: true
description: The rm command is not a builtin and is rejected as not found.
input:
  allowed_commands: ["rm"]
  script: |+
    rm file.txt
expect:
  stdout: ""
  stderr_contains: ["rm: command not found"]
  exit_code: 127

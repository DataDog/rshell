skip_assert_against_bash: true
description: Multiple unknown commands on separate lines all execute and report errors.
input:
  script: |+
    foo
    bar
    baz
expect:
  stdout: ""
  stderr: |+
    command not allowed: foo
    command not allowed: bar
    command not allowed: baz
  exit_code: 1

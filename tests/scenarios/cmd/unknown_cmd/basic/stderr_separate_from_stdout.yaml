description: Unknown command error goes to stderr while stdout remains clean.
input:
  script: |+
    echo before
    nonexistent_cmd
    echo after
expect:
  stdout: |+
    before
    after
  stderr_contains:
    - "nonexistent_cmd"
    - "command not found"
  exit_code: 0

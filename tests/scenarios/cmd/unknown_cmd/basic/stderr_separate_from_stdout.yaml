skip_assert_against_bash: true
description: Unknown command error goes to stderr while stdout remains clean.
input:
  script: |+
    echo before
    nonexistent_cmd
    echo after
expect:
  stdout: |+
    before
    after
  stderr: |+
    command not allowed: nonexistent_cmd
  exit_code: 0

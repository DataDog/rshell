description: An unknown command with underscores in the name prints "command not found".
input:
  allowed_commands: ["my_unknown_command"]
  script: |+
    my_unknown_command
expect:
  stdout: ""
  stderr_contains:
    - "my_unknown_command: command not found"
  exit_code: 127

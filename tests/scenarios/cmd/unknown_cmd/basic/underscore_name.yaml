skip_assert_against_bash: true
description: An unknown command with underscores in the name prints "command not allowed".
input:
  script: |+
    my_unknown_command
expect:
  stdout: ""
  stderr: |+
    command not allowed: my_unknown_command
  exit_code: 1

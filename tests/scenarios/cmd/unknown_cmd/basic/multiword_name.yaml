skip_assert_against_bash: true
description: An unknown command with a multi-word-like name (with hyphens) prints "command not allowed".
input:
  script: |+
    my-unknown-command
expect:
  stdout: ""
  stderr: |+
    command not allowed: my-unknown-command
  exit_code: 1

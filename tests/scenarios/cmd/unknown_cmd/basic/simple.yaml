skip_assert_against_bash: true
description: A simple unknown command prints "command not allowed" to stderr.
input:
  script: |+
    foo
expect:
  stdout: ""
  stderr: |+
    command not allowed: foo
  exit_code: 1

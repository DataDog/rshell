description: The || operator runs the next command after an unknown command.
input:
  allowed_commands: ["echo", "foo"]
  script: |+
    foo || echo fallback
expect:
  stdout: |+
    fallback
  stderr_contains:
    - "foo: command not found"
  exit_code: 0

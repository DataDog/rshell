description: vmstat -a swaps the memory header columns to inact/active instead of buff/cache.
# skip: vmstat is not present in debian:bookworm-slim, and output reflects
# live host state.
skip_assert_against_bash: true
input:
  script: |+
    vmstat -a; echo "code:$?"
expect:
  stdout_contains:
    - "inact"
    - "active"
    - "code:0"
  stderr: ""
  exit_code: 0

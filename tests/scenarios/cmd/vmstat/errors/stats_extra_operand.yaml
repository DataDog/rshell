description: vmstat -s rejects a positional operand (delay/count are meaningless with -s/--stats).
skip_assert_against_bash: true
input:
  script: |+
    vmstat -s 1
expect:
  stdout: ""
  stderr_contains:
    - "vmstat: extra operand"
  exit_code: 1

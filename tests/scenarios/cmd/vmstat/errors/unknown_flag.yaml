description: vmstat exits 1 with an error when given an unknown flag.
skip_assert_against_bash: true
input:
  script: |+
    vmstat --no-such-flag
expect:
  stdout: ""
  stderr_contains: ["vmstat:"]
  exit_code: 1

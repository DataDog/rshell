description: sort outputs a single line with a trailing newline.
setup:
  files:
    - path: file.txt
      content: "only\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sort file.txt
expect:
  stdout: "only\n"
  stderr: ""
  exit_code: 0

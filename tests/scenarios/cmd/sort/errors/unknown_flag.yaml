skip_assert_against_bash: true
description: sort rejects unknown flags with exit code 1 (framework handles flag parse errors).
input:
  allowed_paths: ["$DIR"]
  script: |+
    sort --follow file.txt
expect:
  stdout: ""
  stderr_contains: ["sort:"]
  exit_code: 1

description: sort -c exits 0 when input is sorted.
setup:
  files:
    - path: file.txt
      content: "a\nb\nc\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sort -c file.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

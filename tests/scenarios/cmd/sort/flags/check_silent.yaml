description: sort -C exits 1 with no diagnostic when input is not sorted.
setup:
  files:
    - path: file.txt
      content: "b\na\nc\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sort -C file.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 1

description: "sort rejects incompatible flag combinations matching GNU behavior."
setup:
  files:
    - path: data.txt
      content: "a\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sort -n -d data.txt
expect:
  stdout: ""
  stderr: "sort: options '-dn' are incompatible\n"
  exit_code: 2
  skip_assert_against_bash: true

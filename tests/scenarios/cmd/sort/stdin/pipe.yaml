description: sort reads from stdin when no files are given.
setup:
  files:
    - path: input.txt
      content: "cherry\napple\nbanana\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sort < input.txt
expect:
  stdout: "apple\nbanana\ncherry\n"
  stderr: ""
  exit_code: 0

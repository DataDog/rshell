# Runtime jobs are unavailable outside remediation mode, even when no grant is configured.
skip_assert_against_bash: true
description: systemctl start requires remediation mode
input:
  script: |+
    systemctl start api.service
expect:
  stdout: |+
  stderr: |+
    systemctl: systemd action "start" requires remediation mode
  exit_code: 1

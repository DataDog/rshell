# Derived from GNU coreutils uniq.pl test 124
description: uniq -z uses NUL as line delimiter.
setup:
  files:
    - path: f.txt
      content: "a\x00a\x00b"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq -z f.txt
expect:
  stdout: "a\x00b\x00"
  stderr: ""
  exit_code: 0

# Derived from GNU coreutils uniq.pl test 21
description: uniq -d with no duplicates produces no output.
setup:
  files:
    - path: f.txt
      content: "a\nb\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq -d f.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

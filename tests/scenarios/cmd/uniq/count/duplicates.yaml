# Derived from GNU coreutils uniq.pl test 102
description: uniq -c counts duplicates.
setup:
  files:
    - path: f.txt
      content: "a\na\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq -c f.txt
expect:
  stdout: "      2 a\n"
  stderr: ""
  exit_code: 0

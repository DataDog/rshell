description: uniq with no file arguments reads from standard input.
setup:
  files:
    - path: src.txt
      content: "a\na\nb\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq < src.txt
expect:
  stdout: "a\nb\n"
  stderr: ""
  exit_code: 0

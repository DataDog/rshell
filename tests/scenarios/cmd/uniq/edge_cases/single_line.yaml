description: uniq on single-line file outputs that line.
setup:
  files:
    - path: one.txt
      content: "only\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq one.txt
expect:
  stdout: "only\n"
  stderr: ""
  exit_code: 0

description: uniq -i ignores case when comparing.
setup:
  files:
    - path: input.txt
      content: "A\na\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq -i input.txt
expect:
  stdout: "A\n"
  stderr: ""
  exit_code: 0

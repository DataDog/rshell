# Derived from GNU coreutils uniq.pl test 1
description: uniq with empty input produces no output.
setup:
  files:
    - path: empty.txt
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq empty.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

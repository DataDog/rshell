description: uniq -d -u suppresses all output.
setup:
  files:
    - path: input.txt
      content: "a\na\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq -d -u input.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

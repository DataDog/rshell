# Derived from GNU coreutils uniq.pl test 7
description: uniq with all unique lines passes them through.
setup:
  files:
    - path: f.txt
      content: "a\nb\nc\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq f.txt
expect:
  stdout: "a\nb\nc\n"
  stderr: ""
  exit_code: 0

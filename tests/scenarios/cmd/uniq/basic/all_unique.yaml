description: uniq preserves all unique lines.
setup:
  files:
    - path: input.txt
      content: "a\nb\nc\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq input.txt
expect:
  stdout: "a\nb\nc\n"
  stderr: ""
  exit_code: 0

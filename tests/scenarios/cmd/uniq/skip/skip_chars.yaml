# Derived from GNU coreutils uniq.pl test 42
description: uniq -s 1 skips first character when comparing.
setup:
  files:
    - path: input.txt
      content: "aaa\naaa\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    uniq -s 1 input.txt
expect:
  stdout: "aaa\n"
  stderr: ""
  exit_code: 0

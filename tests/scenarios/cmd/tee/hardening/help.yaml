description: --help prints usage to stdout and exits 0.
input:
  allowed_paths: ["$DIR:rw"]
  mode: "remediation"
  script: |+
    tee --help
expect:
  stdout_contains: ["Usage: tee", "--append"]
  stderr: |+
  exit_code: 0
skip_assert_against_bash: true

description: An empty input file produces no output.
setup:
  files:
    - path: empty.txt
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    awk '{print "x"}' empty.txt
expect:
  stdout: ""
  stderr: |+
  exit_code: 0

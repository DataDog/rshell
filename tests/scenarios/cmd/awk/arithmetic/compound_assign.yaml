description: Compound assignment operators modify a value in place.
input:
  script: |+
    awk 'BEGIN { x = 10; x += 5; print x; x -= 3; print x; x *= 2; print x; x /= 4; print x }'
expect:
  stdout: |+
    15
    12
    24
    6
  stderr: ""
  exit_code: 0

description: Using a scalar variable as an array is a fatal error.
input:
  script: |+
    awk 'BEGIN { x = 1; x[1] = 2; print "after" }'
expect:
  stdout: |+
  stderr: |+
    awk: illegal use of scalar "x" as array
  exit_code: 1
  skip_assert_against_bash: true

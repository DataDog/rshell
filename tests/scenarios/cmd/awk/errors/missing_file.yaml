description: A non-existent input file is reported as an error.
skip_assert_against_bash: true
input:
  allowed_paths: ["$DIR"]
  script: |+
    awk '{print}' nope.txt
expect:
  stdout: ""
  stderr_contains: ["awk: nope.txt"]
  exit_code: 1

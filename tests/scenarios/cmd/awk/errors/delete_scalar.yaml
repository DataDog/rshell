skip_assert_against_bash: true
description: Deleting a scalar variable is a fatal error.
input:
  script: |+
    awk 'BEGIN { x = 1; delete x; print "after" }'
expect:
  stdout: |+
  stderr: |+
    awk: illegal use of scalar "x" as array
  exit_code: 1

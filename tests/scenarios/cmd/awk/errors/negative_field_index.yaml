description: Negative field index (computed at runtime) produces a runtime error (matches gawk behaviour; exit code may differ from bash).
skip_assert_against_bash: true
input:
  script: |+
    echo 'a b c' | awk '{ print $(0-1) }'
expect:
  stdout: |+
  stderr_contains: ["negative field index"]
  exit_code: 1

# END block
description: awk END block executes after input processing.
setup:
  files:
    - path: file.txt
      content: "a\nb\nc\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    awk '{count++} END {print count}' file.txt
expect:
  stdout: "3\n"
  stderr: ""
  exit_code: 0

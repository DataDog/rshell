description: awk POSIX letter classes match Unicode letters.
input:
  script: |+
    awk 'BEGIN { print match("é", /[[:alpha:]]/), match("é", /[x[:alnum:]]/), match("é", /[[:lower:]]/), match("É", /[[:upper:]]/) }'
expect:
  stdout: |+
    1 1 1 1
  stderr: |+
  exit_code: 0

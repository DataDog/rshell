description: awk treats malformed dynamic regular expressions as fatal.
input:
  script: |+
    awk 'BEGIN { pattern="["; print "a" ~ pattern }'
expect:
  stdout: |+
  stderr: |+
    awk: fatal: invalid regular expression "[": error parsing regexp: missing closing ]: `[)`
  exit_code: 2

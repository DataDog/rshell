description: awk print accepts a regex literal as an expression.
setup:
  files:
    - path: input.txt
      content: |+
        foo
        bar
input:
  allowed_paths: ["$DIR"]
  script: |+
    awk '{ print /foo/ }' input.txt
expect:
  stdout: |+
    1
    0
  stderr: |+
  exit_code: 0

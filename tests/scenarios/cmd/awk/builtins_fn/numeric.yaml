description: int(), sqrt(), and exp() return expected values.
input:
  script: |+
    awk 'BEGIN { printf "%d %.4f %.4f\n", int(3.9), sqrt(2), exp(1) }'
expect:
  stdout: |+
    3 1.4142 2.7183
  stderr: ""
  exit_code: 0

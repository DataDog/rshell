description: The 'in' operator tests array membership.
input:
  script: |+
    awk 'BEGIN {
      a["x"]=1
      print ("x" in a), ("y" in a)
    }'
expect:
  stdout: |+
    1 0
  stderr: |+
  exit_code: 0

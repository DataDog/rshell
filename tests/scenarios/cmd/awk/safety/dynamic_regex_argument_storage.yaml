# skip: rshell intentionally bounds retained builtin argument values.
skip_assert_against_bash: true
description: awk charges evaluated dynamic regex patterns to aggregate argument storage.
input:
  script: |+
    awk 'BEGIN { substr(sprintf("%1000000s", ""), match("", sprintf("%60000s", ""))) }'
expect:
  stdout: |+
  stderr: |+
    awk: function argument storage limit exceeded (maximum 1048576 bytes)
  exit_code: 1

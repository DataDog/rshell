description: awk command pipes reject shell syntax blocked by rshell.
skip_assert_against_bash: true
input:
  script: |+
    awk 'BEGIN { print "payload" | "echo $((1 + 1))" }'
expect:
  stdout: |+
  stderr: |+
    awk: arithmetic expansion is not supported
  exit_code: 1

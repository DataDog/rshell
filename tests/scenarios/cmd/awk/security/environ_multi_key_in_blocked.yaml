description: Multi-key (a,b) in ENVIRON membership test is rejected at parse time.
skip_assert_against_bash: true
input:
  script: |+
    awk 'BEGIN { if (("a","b") in ENVIRON) print "yes" }'
expect:
  stdout: |+
  stderr_contains: ["ENVIRON"]
  exit_code: 1

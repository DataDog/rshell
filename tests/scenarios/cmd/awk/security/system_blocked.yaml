description: system() is blocked at parse time.
skip_assert_against_bash: true
input:
  script: |+
    awk 'BEGIN { system("echo pwned") }'
expect:
  stdout: ""
  stderr_contains: ["system()"]
  exit_code: 1

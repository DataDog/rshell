description: print >> "file" append redirection is blocked.
skip_assert_against_bash: true
input:
  script: |+
    awk 'BEGIN { print "x" >> "/tmp/awk_pwn" }'
expect:
  stdout: ""
  stderr_contains: ["output redirection"]
  exit_code: 1

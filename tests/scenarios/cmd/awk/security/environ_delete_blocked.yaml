description: delete ENVIRON is rejected at parse time.
skip_assert_against_bash: true
input:
  script: |+
    awk 'BEGIN { delete ENVIRON }'
expect:
  stdout: ""
  stderr_contains: ["ENVIRON"]
  exit_code: 1

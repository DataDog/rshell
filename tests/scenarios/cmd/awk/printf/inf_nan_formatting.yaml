description: printf %f/%g/%e with Inf/NaN produces lowercase gawk-compatible output.
skip_assert_against_bash: true
input:
  script: |+
    awk -v x=1e9999 'BEGIN { printf "%f %g %e\n", x, x, x }'
expect:
  stdout: |+
    +inf +inf +inf
  stderr: |+
  exit_code: 0

description: break exits a loop; continue skips to next iteration.
input:
  script: |+
    awk 'BEGIN {
      for (i = 1; i <= 5; i++) {
        if (i == 3) continue
        if (i == 5) break
        print i
      }
    }'
expect:
  stdout: |+
    1
    2
    4
  stderr: |+
  exit_code: 0

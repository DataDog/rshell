description: for (var in array) iterates over keys.
input:
  script: |+
    awk 'BEGIN {
      a["x"] = 1
      a["y"] = 2
      a["z"] = 3
      n = 0
      for (k in a) n += a[k]
      print n
    }'
expect:
  stdout: |+
    6
  stderr: |+
  exit_code: 0

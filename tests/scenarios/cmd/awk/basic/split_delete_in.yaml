description: awk split, delete, and in support array membership checks.
oracle: gawk
input:
  script: |+
    awk 'BEGIN { split("a,b:c", f, /[,:]/); counts[f[1]]++; counts[f[2]]++; delete counts["b"]; print (f[1] in counts), ("b" in counts), f[3] }'
expect:
  stdout: |+
    1 0 c
  stderr: |+
  exit_code: 0

description: awk preserves malformed UTF-8 bytes embedded directly in program source.
input:
  script: |+
    program="$(printf 'BEGIN { print ("A\377B" == "A\\377B"), ("A\\377B" ~ /A\377B/) }')"
    awk "$program"
expect:
  stdout: |+
    1 1
  stderr: |+
  exit_code: 0

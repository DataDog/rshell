description: awk treats an empty program as a no-op.
skip_assert_against_bash: true
input:
  allowed_paths: ["$DIR"]
  script: |+
    awk '' missing.txt
expect:
  stdout: |+
  stderr: |+
  exit_code: 0

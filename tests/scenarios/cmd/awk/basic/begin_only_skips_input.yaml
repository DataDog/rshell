description: awk defers input-only work for BEGIN-only programs.
input:
  allowed_paths: ["$DIR"]
  script: |+
    awk 'BEGIN { print "x" }' missing.txt
    awk 'BEGIN { FS="[["; FS=":"; print 1 }'
expect:
  stdout: |+
    x
    1
  stderr: |+
  exit_code: 0

description: jq raw mode treats each input line as a string and writes strings without JSON quotes.
input:
  script: |+
    printf 'one\ntwo\n' | jq -Rr '.'
expect:
  stdout: |+
    one
    two
  stderr: |+
  exit_code: 0

# skip: lsof is not installed by default in the debian:bookworm-slim
# comparison image, and this builtin's flag set is a custom subset of real
# lsof (see builtins/lsof's package doc comment).
description: lsof --and=false is rejected — -a/--and takes no argument, matching GNU getopt no-argument semantics (df's/free's noArgBool pattern)
skip_assert_against_bash: true
input:
  script: |+
    lsof --and=false
expect:
  stdout: |+
  stderr: |+
    lsof: option '--and' doesn't allow an argument
    Try 'lsof --help' for more information.
  exit_code: 1

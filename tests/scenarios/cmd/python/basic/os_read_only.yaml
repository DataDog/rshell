description: python os.getenv always returns the default — host environment is not accessible.
skip_assert_against_bash: true
input:
  script: |+
    python -c "import os; print(os.getenv('NONEXISTENT_KEY_XYZ', 'default'))"
expect:
  stdout: |+
    default
  stderr: |+
  exit_code: 0

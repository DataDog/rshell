description: open() in append mode raises PermissionError.
skip_assert_against_bash: true
input:
  script: |+
    python -c "open('/tmp/evil.txt', 'a')"
expect:
  stdout: |+
  stderr_contains: ["PermissionError"]
  exit_code: 1

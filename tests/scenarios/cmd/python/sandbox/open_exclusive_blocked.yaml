description: open() in exclusive creation mode ('x') raises PermissionError.
skip_assert_against_bash: true
input:
  script: |+
    python -c "open('/tmp/new.txt', 'x')"
expect:
  stdout: |+
  stderr_contains: ["PermissionError"]
  exit_code: 1

description: os.symlink() is blocked and raises AttributeError.
skip_assert_against_bash: true
input:
  script: |+
    python -c "import os; os.symlink('/tmp/src', '/tmp/dst')"
expect:
  stdout: |+
  stderr_contains: ["AttributeError"]
  exit_code: 1

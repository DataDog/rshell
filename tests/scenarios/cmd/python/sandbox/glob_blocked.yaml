description: glob module is blocked and raises ImportError when imported.
skip_assert_against_bash: true
input:
  script: |+
    python -c "import glob; glob.glob('*')"
expect:
  stdout: |+
  stderr_contains: ["ImportError"]
  exit_code: 1

description: tempfile module is blocked and raises ImportError when imported.
skip_assert_against_bash: true
input:
  script: |+
    python -c "import tempfile; tempfile.mkstemp()"
expect:
  stdout: |+
  stderr_contains: ["ImportError"]
  exit_code: 1

description: os.unlink() is blocked and raises AttributeError.
skip_assert_against_bash: true
input:
  script: |+
    python -c "import os; os.unlink('/tmp/test')"
expect:
  stdout: |+
  stderr_contains: ["AttributeError"]
  exit_code: 1

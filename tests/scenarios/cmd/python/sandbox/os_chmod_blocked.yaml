description: os.chmod() is blocked and raises AttributeError.
skip_assert_against_bash: true
input:
  script: |+
    python -c "import os; os.chmod('/tmp/test', 0o755)"
expect:
  stdout: |+
  stderr_contains: ["AttributeError"]
  exit_code: 1

description: printf --help prints usage to stdout and exits 2.
# skip: rshell builtin help output differs from GNU coreutils; also uses exit 2
skip_assert_against_bash: true
input:
  script: |+
    printf --help
expect:
  stdout_contains: ["printf"]
  stderr: ""
  exit_code: 2

description: Printf with only a format string and no arguments prints the format string.
input:
  script: |+
    printf "hello world\n"
expect:
  stdout: |+
    hello world
  stderr: ""
  exit_code: 0

description: tr rejects STRING2 ending with character class when STRING1 is longer.
skip_assert_against_bash: true
input:
  script: |+
    tr '[:lower:]x' '[:upper:]'
expect:
  stdout: ""
  stderr_contains: ["the latter string must not end with a character class"]
  exit_code: 1

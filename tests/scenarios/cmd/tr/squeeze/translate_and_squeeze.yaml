# Derived from uutils test_translate_and_squeeze
description: tr -s translates then squeezes output.
input:
  script: |+
    cat <<'EOF' | tr -s x y
    xx
    EOF
expect:
  stdout: "y\n"
  stderr: ""
  exit_code: 0

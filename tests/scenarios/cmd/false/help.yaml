description: False --help displays usage information.
skip_assert_against_bash: true  # bash builtin false ignores --help; we match GNU coreutils
input:
  script: |+
    false --help
expect:
  stdout: |+
    Usage: false
    Exit with a status code indicating failure.
  stderr: ""
  exit_code: 1

description: truncate --help in read-only mode is rejected before help is displayed.
input:
  allowed_paths: ["$DIR"]
  script: |+
    truncate --help
expect:
  stdout: |+
  stderr: |+
    truncate: command requires remediation mode
  exit_code: 1
skip_assert_against_bash: true

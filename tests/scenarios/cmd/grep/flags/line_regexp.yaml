description: grep -x matches only whole lines.
setup:
  files:
    - path: file.txt
      content: |+
        foo
        foo bar
        bar foo
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    grep -x foo file.txt
expect:
  stdout: |+
    foo
  stderr: ""
  exit_code: 0

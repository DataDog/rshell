description: grep -E with alternation matches multiple patterns.
setup:
  files:
    - path: input.txt
      content: "apple\nbanana\ncherry\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    grep -E 'apple|cherry' input.txt
expect:
  stdout: "apple\ncherry\n"
  stderr: ""
  exit_code: 0

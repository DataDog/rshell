description: grep -B prints lines before each match.
setup:
  files:
    - path: file.txt
      content: |+
        aaa
        bbb
        ccc
        ddd
        eee
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    grep -B 1 ccc file.txt
expect:
  stdout: |+
    bbb
    ccc
  stderr: ""
  exit_code: 0

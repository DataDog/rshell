description: pwd accepts -- as the end-of-flags separator and ignores trailing positional args.
skip_assert_against_bash: true
input:
  allowed_paths: ["$DIR"]
  script: |+
    pwd -- --not-a-flag
expect:
  stdout_contains: ["/"]
  stdout_contains_windows: ["\\"]
  stderr: ""
  exit_code: 0

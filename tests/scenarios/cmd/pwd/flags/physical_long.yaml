description: pwd --physical is an alias for -P.
skip_assert_against_bash: true
input:
  allowed_paths: ["$DIR"]
  script: |+
    pwd --physical
expect:
  stdout_contains: ["/"]
  stdout_contains_windows: ["\\"]
  stderr: ""
  exit_code: 0

description: cat on an empty file produces no output and exits 0.
setup:
  files:
    - path: empty.txt
      content: ""
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    cat empty.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

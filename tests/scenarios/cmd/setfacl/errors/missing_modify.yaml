# skip: rshell setfacl is a narrow POSIX ACL helper, not GNU setfacl; there
# is no real `setfacl` binary in the debian:bookworm-slim comparison image
# to compare against.
description: setfacl requires -m/--modify
skip_assert_against_bash: true
setup:
  files:
    - path: app
      content: "x"
input:
  allowed_paths: ["$DIR:rw"]
  mode: "remediation"
  script: |+
    setfacl app
expect:
  stdout: |+
  stderr: |+
    setfacl: you must specify -m/--modify
  exit_code: 1

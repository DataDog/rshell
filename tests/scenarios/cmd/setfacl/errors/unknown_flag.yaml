# skip: rshell setfacl is a narrow POSIX ACL helper, not GNU setfacl; there
# is no real `setfacl` binary in the debian:bookworm-slim comparison image
# to compare against.
description: setfacl rejects unknown flags
skip_assert_against_bash: true
input:
  allowed_paths: ["$DIR:rw"]
  mode: "remediation"
  script: |+
    setfacl --bogus
expect:
  stdout: |+
  stderr: |+
    setfacl: unrecognized option '--bogus'
    Try 'setfacl --help' for more information.
  exit_code: 1

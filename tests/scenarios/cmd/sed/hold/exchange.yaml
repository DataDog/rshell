description: The x command exchanges pattern and hold space.
setup:
  files:
    - path: input.txt
      content: "first\nsecond\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed -n '1{h;d}; 2{x;p;x;p}' input.txt
expect:
  stdout: "first\nsecond\n"
  stderr: ""
  exit_code: 0

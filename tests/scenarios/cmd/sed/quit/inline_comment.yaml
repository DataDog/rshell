description: "Inline # comment after q/Q is accepted."
setup:
  files:
    - path: input.txt
      content: "line1\nline2\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed 'q#note' input.txt
expect:
  stdout: "line1\n"
  exit_code: 0

description: Sed reports an error when no script is provided.
input:
  script: |+
    sed
# skip: rshell builtin error/output format differs from GNU coreutils
skip_assert_against_bash: true
expect:
  stdout: ""
  stderr: "sed: no script command has been specified\n"
  exit_code: 1

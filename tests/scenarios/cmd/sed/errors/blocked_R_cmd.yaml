description: The R command is blocked for safety.
# skip: feature is intentionally blocked in rshell for security
skip_assert_against_bash: true
input:
  script: |+
    echo "test" | sed 'R input.txt'
expect:
  stdout: ""
  stderr: "sed: 'R' command is blocked: unsandboxed file reading is not allowed\n"
  exit_code: 1

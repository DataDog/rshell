description: The w command is blocked for safety.
skip_assert_against_bash: true
input:
  script: |+
    echo "test" | sed 'w output.txt'
expect:
  stdout: ""
  stderr: "sed: 'w' command is blocked: file writing is not allowed\n"
  exit_code: 1

description: "Empty regex address // with no previous regex produces an error."
input:
  script: |+
    echo test | sed -n '//p'
# skip: rshell builtin error/output format differs from GNU coreutils
skip_assert_against_bash: true
expect:
  stdout: ""
  stderr_contains: ["no previous regular expression"]
  exit_code: 1

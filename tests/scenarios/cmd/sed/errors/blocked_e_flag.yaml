description: The e flag in s command is blocked for safety.
skip_assert_against_bash: true
input:
  script: |+
    echo "test" | sed 's/test/replaced/e'
expect:
  stdout: ""
  stderr: "sed: 'e' flag in 's' command is blocked: command execution is not allowed\n"
  exit_code: 1

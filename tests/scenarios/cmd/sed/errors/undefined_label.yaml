description: Undefined branch labels are rejected at parse time.
skip_assert_against_bash: true
input:
  script: |+
    echo 'hello' | sed 'bmissing'
expect:
  stdout: ""
  stderr_contains: ["undefined label"]
  exit_code: 1

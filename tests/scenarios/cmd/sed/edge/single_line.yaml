description: Sed processes a single line file correctly.
setup:
  files:
    - path: input.txt
      content: "only line\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed 's/only/the only/' input.txt
expect:
  stdout: "the only line\n"
  stderr: ""
  exit_code: 0

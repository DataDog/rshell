description: "A branch command inside a group skips commands after the group."
setup:
  files:
    - path: input.txt
      content: "a\nb\nc\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed -n '{b};p' input.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

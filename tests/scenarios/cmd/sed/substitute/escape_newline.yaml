description: The \n escape in replacement inserts a literal newline.
setup:
  files:
    - path: input.txt
      content: "hello world\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed 's/ /\n/' input.txt
expect:
  stdout: "hello\nworld\n"
  stderr: ""
  exit_code: 0

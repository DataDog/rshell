description: "Empty pattern in s/// with no previous regex produces an error."
input:
  script: |+
    echo test | sed 's//X/'
skip_assert_against_bash: true
expect:
  stdout: ""
  stderr_contains: ["no previous regular expression"]
  exit_code: 1

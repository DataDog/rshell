description: N at EOF prints pattern space and exits gracefully.
setup:
  files:
    - path: input.txt
      content: "a\nb\nc\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed 'N' input.txt
expect:
  stdout: "a\nb\nc\n"
  exit_code: 0

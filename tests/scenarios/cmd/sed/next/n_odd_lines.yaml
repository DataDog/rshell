description: The n command on odd number of lines prints all lines.
setup:
  files:
    - path: input.txt
      content: "a\nb\nc\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed 'n' input.txt
expect:
  stdout: "a\nb\nc\n"
  exit_code: 0

description: The c command replaces the current line with new text.
setup:
  files:
    - path: input.txt
      content: "line1\nline2\nline3\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed '2c replaced' input.txt
expect:
  stdout: "line1\nreplaced\nline3\n"
  stderr: ""
  exit_code: 0

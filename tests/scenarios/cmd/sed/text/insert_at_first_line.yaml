description: Sed i command inserts text before the first line.
setup:
  files:
    - path: input.txt
      content: "line1\nline2\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed '1i header' input.txt
expect:
  stdout: "header\nline1\nline2\n"
  stderr: ""
  exit_code: 0

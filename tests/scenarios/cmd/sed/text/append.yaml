description: The a command appends text after the current line.
setup:
  files:
    - path: input.txt
      content: "line1\nline2\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    sed '1a appended' input.txt
expect:
  stdout: "line1\nappended\nline2\n"
  stderr: ""
  exit_code: 0

description: Sed reads from stdin when - is given as the file argument.
input:
  script: |+
    echo "hello world" | sed 's/world/earth/' -
expect:
  stdout: "hello earth\n"
  stderr: ""
  exit_code: 0

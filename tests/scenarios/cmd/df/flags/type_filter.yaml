description: df -t with a type that doesn't exist still prints the header and exits 0
skip_assert_against_bash: true
input:
  script: |+
    df -t no-such-fs-type | wc -l
expect:
  stdout: |+
    1
  stderr: ""
  exit_code: 0

description: test -r returns 0 for a readable file.
setup:
  files:
    - path: file.txt
      content: "hello"
input:
  allowed_paths: ["$DIR"]
  script: |+
    test -r file.txt && echo yes
expect:
  stdout: "yes\n"
  stderr: ""
  exit_code: 0

description: test -s returns 1 for an empty file.
setup:
  files:
    - path: empty.txt
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    test -s empty.txt && echo yes || echo no
expect:
  stdout: "no\n"
  stderr: ""
  exit_code: 0

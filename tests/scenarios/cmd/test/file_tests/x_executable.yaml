description: test -x returns 0 for an executable file.
setup:
  files:
    - path: script
      content: "#!/bin/sh"
      chmod: 0755
input:
  allowed_paths: ["$DIR"]
  script: |+
    test -x script && echo yes
expect:
  stdout: "yes\n"
  stderr: ""
  exit_code: 0

# Derived from uutils test_test.rs — test -s (size greater than zero)
description: "test -s on empty and non-empty files"
setup:
  files:
    - path: empty.txt
      content: ""
    - path: nonempty.txt
      content: "data"
input:
  allowed_paths: ["$DIR"]
  script: |+
    test -s nonempty.txt
    echo "nonempty: $?"
    test -s empty.txt
    echo "empty: $?"
    test -s nonexistent
    echo "missing: $?"
expect:
  stdout: |
    nonempty: 0
    empty: 1
    missing: 1
  stderr: ""
  exit_code: 0
skip_assert_against_bash: true

# Derived from GNU coreutils test.pl test 1b — -z on empty string
description: "test -z on empty string returns true"
input:
  script: |+
    test -z ""
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

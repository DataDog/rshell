# Derived from GNU coreutils test.pl test 1e — empty string is false
description: "test with empty string argument returns false"
input:
  script: |+
    test ""
expect:
  stdout: ""
  stderr: ""
  exit_code: 1

# Derived from GNU coreutils test.pl — empty test returns false
description: "test with no arguments returns false (exit 1)"
input:
  script: |+
    test
expect:
  stdout: ""
  stderr: ""
  exit_code: 1

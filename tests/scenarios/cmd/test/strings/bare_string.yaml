# Derived from GNU coreutils test.pl test 1c — bare string is true
description: "test with a non-empty string returns true"
input:
  script: |+
    test "any-string"
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

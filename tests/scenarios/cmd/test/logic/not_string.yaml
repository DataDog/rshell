description: test ! negates string truth value.
input:
  allowed_paths: ["$DIR"]
  script: |+
    test ! "" && echo yes; test ! "A" && echo bad || echo no
expect:
  stdout: "yes\nno\n"
  stderr: ""
  exit_code: 0

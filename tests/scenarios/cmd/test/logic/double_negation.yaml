description: test ! ! double negation restores original truth value.
input:
  allowed_paths: ["$DIR"]
  script: |+
    test ! ! "a" && echo yes; test ! ! "" && echo bad || echo no
expect:
  stdout: "yes\nno\n"
  stderr: ""
  exit_code: 0

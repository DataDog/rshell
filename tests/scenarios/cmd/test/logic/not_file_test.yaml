description: test ! negates file test operators.
setup:
  files:
    - path: file.txt
      content: "hello"
input:
  allowed_paths: ["$DIR"]
  script: |+
    test ! -d file.txt && echo yes; test ! -f file.txt && echo bad || echo no
expect:
  stdout: "yes\nno\n"
  stderr: ""
  exit_code: 0

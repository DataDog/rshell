description: ls --help prints usage information and exits with code 0.
skip_assert_against_bash: true
input:
  script: |+
    ls --help
expect:
  stdout_contains:
    - "Usage: ls"
    - "FILE"
  exit_code: 0

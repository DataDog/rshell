description: True --help displays usage information.
skip_assert_against_bash: true  # bash builtin true ignores --help; we match GNU coreutils
input:
  script: |+
    true --help
expect:
  stdout: |+
    Usage: true
    Exit with a status code indicating success.
  stderr: ""
  exit_code: 0

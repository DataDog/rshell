# Derived from GNU coreutils wc.pl test a0
description: wc -c on empty file outputs 0.
setup:
  files:
    - path: empty.txt
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    wc -c empty.txt
expect:
  stdout: "0 empty.txt\n"
  stderr: ""
  exit_code: 0

description: wc exits 1 and prints error for empty filename argument.
skip_assert_against_bash: true  # rshell returns a clearer error than GNU wc for empty filenames
input:
  script: |+
    wc ""
expect:
  stdout: ""
  stderr: "wc: invalid zero-length file name\n"
  exit_code: 1

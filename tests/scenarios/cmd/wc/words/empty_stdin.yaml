description: wc -w on empty file outputs 0.
setup:
  files:
    - path: empty.txt
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    wc -w empty.txt
expect:
  stdout: "0 empty.txt\n"
  stderr: ""
  exit_code: 0

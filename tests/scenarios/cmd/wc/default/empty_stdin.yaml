description: wc with no args and empty stdin outputs all zeros.
setup:
  files:
    - path: empty.txt
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    wc empty.txt
expect:
  stdout: "0 0 0 empty.txt\n"
  stderr: ""
  exit_code: 0

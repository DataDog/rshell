description: wc -- treats dash-prefixed filenames as operands.
setup:
  files:
    - path: "-secret.txt"
      content: "x\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    wc -- -secret.txt
expect:
  stdout: "1 1 2 -secret.txt\n"
  stderr: ""
  exit_code: 0

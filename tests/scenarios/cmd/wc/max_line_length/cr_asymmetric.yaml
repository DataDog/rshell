description: wc -L preserves max line length across carriage return reset.
setup:
  files:
    - path: file.txt
      content: "abcdef\rxy\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    wc -L file.txt
expect:
  stdout: "6 file.txt\n"
  stderr: ""
  exit_code: 0

description: wc -L picks the max among multiple lines.
setup:
  files:
    - path: file.txt
      content: "1\n123\n1\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    wc -L file.txt
expect:
  stdout: "3 file.txt\n"
  stderr: ""
  exit_code: 0

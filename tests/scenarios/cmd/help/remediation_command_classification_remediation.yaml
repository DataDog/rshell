description: Help in remediation mode moves allowed remediation-only commands into the commands available now table while policy-disabled commands remain separate.
skip_assert_against_bash: true
input:
  mode: "remediation"
  allowed_commands: ["rshell:echo", "rshell:help", "rshell:rm", "rshell:truncate"]
  script: |+
    help
expect:
  stdout_contains:
    - "remediation mode"
    - "Commands available now (4):"
    - "rm        remove files"
    - "truncate  shrink or extend file size"
    - "Disabled by policy ("
  stderr: ""
  exit_code: 0

description: du -d 0 -b prints only the operand (no children).
setup:
  files:
    - path: top/a.txt
      content: "abcde"
    - path: top/sub/inner.txt
      content: "xy"
input:
  allowed_paths: ["$DIR"]
  script: |+
    du -d 0 -b top
expect:
  stdout: |+
    7	top
  stderr: ""
  exit_code: 0

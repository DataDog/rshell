# -I (bind to interface) is rejected as an unknown flag.
description: "ping -I (interface) is rejected with exit 1"
input:
  script: |+
    ping -I eth0 localhost
expect:
  stdout: ""
  stderr: "ping: unknown shorthand flag: 'I' in -I\n"
  exit_code: 1
skip_assert_against_bash: true

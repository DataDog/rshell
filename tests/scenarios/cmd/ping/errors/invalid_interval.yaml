# ping is not a bash builtin — skip bash comparison
skip_assert_against_bash: true
description: ping -i 0 rejects zero interval with error.
input:
  script: |+
    ping -i 0 localhost
expect:
  stdout: ""
  stderr_contains: ["ping: invalid interval"]
  exit_code: 1

# ping is not a bash builtin — skip bash comparison
skip_assert_against_bash: true
description: ping -c 0 rejects zero count with error.
input:
  script: |+
    ping -c 0 localhost
expect:
  stdout: ""
  stderr_contains: ["ping: invalid count"]
  exit_code: 1

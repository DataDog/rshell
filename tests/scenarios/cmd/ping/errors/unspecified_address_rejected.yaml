# Pinging 0.0.0.0 (unspecified address) must be rejected.
description: "ping 0.0.0.0 (unspecified address) exits 1"
input:
  script: |+
    ping -c 1 -W 100ms 0.0.0.0
expect:
  stderr: "ping: unspecified destination not allowed: 0.0.0.0\n"
  exit_code: 1
skip_assert_against_bash: true

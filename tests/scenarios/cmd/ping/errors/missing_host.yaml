# ping is not a bash builtin — skip bash comparison
skip_assert_against_bash: true
description: ping with no arguments prints error and exits 1.
input:
  script: |+
    ping
expect:
  stdout: ""
  stderr_contains: ["ping: missing host operand"]
  exit_code: 1

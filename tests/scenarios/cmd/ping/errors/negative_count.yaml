# ping is not a bash builtin — skip bash comparison
skip_assert_against_bash: true
description: ping -c -1 rejects negative count with error.
input:
  script: |+
    ping -c -1 localhost
expect:
  stdout: ""
  stderr_contains: ["ping: invalid count"]
  exit_code: 1

skip_assert_against_bash: true
description: ping -c 0 rejects zero count with exit code 1.
input:
  script: |+
    ping -c 0 localhost
expect:
  stdout: ""
  stderr_contains: ["ping: invalid count"]
  exit_code: 1

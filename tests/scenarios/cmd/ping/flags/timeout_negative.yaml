skip_assert_against_bash: true
description: ping -W -1 rejects negative timeout with exit code 1.
input:
  script: |+
    ping -W -1 localhost
expect:
  stdout: ""
  stderr_contains: ["ping: invalid timeout"]
  exit_code: 1

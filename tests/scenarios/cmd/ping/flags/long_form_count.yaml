skip_assert_against_bash: true
description: ping --count 0 rejects zero count via long form flag with exit code 1.
input:
  script: |+
    ping --count 0 localhost
expect:
  stdout: ""
  stderr_contains: ["ping: invalid count"]
  exit_code: 1

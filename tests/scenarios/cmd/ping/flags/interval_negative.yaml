skip_assert_against_bash: true
description: ping -i -1 rejects negative interval with exit code 1.
input:
  script: |+
    ping -i -1 localhost
expect:
  stdout: ""
  stderr_contains: ["ping: invalid interval"]
  exit_code: 1

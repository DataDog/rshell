skip_assert_against_bash: true
description: ping --timeout 0 rejects zero timeout via long form flag with exit code 1.
input:
  script: |+
    ping --timeout 0 localhost
expect:
  stdout: ""
  stderr_contains: ["ping: invalid timeout"]
  exit_code: 1

# skip: output depends on live kernel socket state which differs between hosts.
description: ss -n disables name/port resolution and exits 0.
skip_assert_against_bash: true
input:
  allowed_paths: ["/proc/net"]
  script: |+
    ss -n; echo "exit:$?"
expect:
  stdout_contains:
    - "Netid"
    - "exit:0"
  stderr: ""
  exit_code: 0

# skip: output depends on live kernel socket state which differs between hosts.
description: ss -tul combines TCP, UDP, and listening filters and exits 0.
skip_assert_against_bash: true
input:
  allowed_paths: ["/proc/net"]
  script: |+
    ss -tul; echo "exit:$?"
expect:
  stdout_contains:
    - "Netid"
    - "exit:0"
  stderr: ""
  exit_code: 0

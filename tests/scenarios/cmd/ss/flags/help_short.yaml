description: ss -h prints usage information and exits with code 0.
# skip: rshell builtin help output differs from the real ss(8) utility
skip_assert_against_bash: true
input:
  script: |+
    ss -h
expect:
  stdout_contains:
    - "Usage: ss"
  stderr: ""
  exit_code: 0

description: Default behavior lists symlinks as-is without following.
setup:
  files:
    - path: dir/target.txt
      content: "target"
      chmod: 0644
    - path: dir/link.txt
      symlink: target.txt
input:
  allowed_paths: ["$DIR"]
  script: |+
    find dir
expect:
  stdout_unordered: |+
    dir
    dir/link.txt
    dir/target.txt
  stderr: ""
  exit_code: 0

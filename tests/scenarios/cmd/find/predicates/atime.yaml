description: find -amin matches files accessed within specified minutes.
skip_assert_against_bash: true
setup:
  files:
    - path: dir/recent.txt
      content: "just created"
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    find dir -type f -amin -60
expect:
  stdout: |+
    dir/recent.txt
  stderr: ""
  exit_code: 0

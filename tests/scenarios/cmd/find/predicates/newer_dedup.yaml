description: duplicate -newer refs produce only one error line.
skip_assert_against_bash: true
setup:
  files:
    - path: dir/a.txt
      content: "a"
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    find dir -newer nonexist -o -newer nonexist
expect:
  stdout: ""
  stderr_contains: ["find: 'nonexist'"]
  exit_code: 1

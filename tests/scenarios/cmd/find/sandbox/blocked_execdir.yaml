description: find -execdir executes commands from the file's parent directory.
skip_assert_against_bash: true  # rshell -execdir only runs builtins, not external binaries
setup:
  files:
    - path: dummy.txt
      content: "x"
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    find . -name "dummy.txt" -execdir echo found {} \;
expect:
  stdout: "found ./dummy.txt\n"
  exit_code: 0

description: find -exec echo {} \; -print — both the exec echo and -print run for each file.
skip_assert_against_bash: true  # rshell -exec only runs builtins, not external commands
setup:
  files:
    - path: dir/a.txt
      content: "a"
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  allowed_commands:
    - rshell:find
    - rshell:echo
  script: |+
    find dir -name '*.txt' -exec echo {} \; -print
expect:
  stdout: |+
    dir/a.txt
    dir/a.txt
  stderr: ""
  exit_code: 0

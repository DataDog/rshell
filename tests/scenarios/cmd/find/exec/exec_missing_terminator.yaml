description: "find -exec without terminator produces an error"
setup:
  files:
    - path: dummy.txt
      content: "x"
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    find . -exec echo {}
expect:
  stderr_contains: ["missing argument"]
  exit_code: 1

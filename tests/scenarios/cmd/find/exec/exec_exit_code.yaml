description: "find -exec returns true when command exits 0, false otherwise"
setup:
  files:
    - path: a.txt
      content: "a"
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    find . -name 'a.txt' -exec true \; -print
expect:
  stdout: "./a.txt\n"
  exit_code: 0

skip_assert_against_bash: true
description: Echo treats -n as a literal argument, not as an option.
input:
  script: |+
    echo -n hello
expect:
  stdout: |+
    -n hello
  stderr: ""
  exit_code: 0

# skip: rshell error message differs from bash ("too many arguments" format differs).
skip_assert_against_bash: true
description: break with too many arguments prints error to stderr, exits 1, and loop continues.
input:
  script: |+
    for i in 1 2 3; do
      break 1 2
      echo "after: $?"
    done
    echo done
expect:
  stdout: |+
    after: 1
    after: 1
    after: 1
    done
  stderr: |+
    break: too many arguments
    break: too many arguments
    break: too many arguments
  exit_code: 0

description: strings on an empty file produces no output and exits 0.
skip_assert_against_bash: true
setup:
  files:
    - path: empty.bin
      content: ""
input:
  allowed_paths: ["$DIR"]
  script: |+
    strings empty.bin
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

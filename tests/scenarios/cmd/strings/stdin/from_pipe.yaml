description: strings reads from stdin when no file arguments are given.
skip_assert_against_bash: true
input:
  script: |+
    echo "hello world" | strings
expect:
  stdout: |+
    hello world
  stderr: ""
  exit_code: 0

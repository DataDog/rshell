skip_assert_against_bash: true
description: tcpdump -Z (run as different user) is rejected with exit code 1.
input:
  script: |+
    tcpdump -Z root
expect:
  stdout: ""
  stderr_contains: ["tcpdump:"]
  exit_code: 1

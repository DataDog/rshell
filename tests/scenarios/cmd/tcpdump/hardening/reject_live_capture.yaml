skip_assert_against_bash: true
description: tcpdump -i (live capture) is rejected with exit code 1.
input:
  script: |+
    tcpdump -i eth0
expect:
  stdout: ""
  stderr_contains: ["tcpdump:"]
  exit_code: 1

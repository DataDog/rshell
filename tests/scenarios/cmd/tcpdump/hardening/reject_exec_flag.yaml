skip_assert_against_bash: true
description: tcpdump -z (execute postrotate command) is rejected with exit code 1.
input:
  script: |+
    tcpdump -z /bin/sh
expect:
  stdout: ""
  stderr_contains: ["tcpdump:"]
  exit_code: 1

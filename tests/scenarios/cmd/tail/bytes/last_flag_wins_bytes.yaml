description: When -n and -c are both given, the last flag wins; -c last means byte mode.
setup:
  files:
    - path: file.txt
      content: "hello\nworld\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail -n 2 -c 6 file.txt
expect:
  stdout: "world\n"
  stderr: ""
  exit_code: 0

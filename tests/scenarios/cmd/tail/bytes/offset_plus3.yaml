description: tail -c +3 outputs all bytes starting from byte 3 (skip first 2).
setup:
  files:
    - path: file.txt
      content: "hello"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail -c +3 file.txt
expect:
  stdout: "llo"
  stderr: ""
  exit_code: 0

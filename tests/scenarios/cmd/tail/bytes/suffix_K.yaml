description: tail -c 1K outputs the last 1024 bytes (K suffix = 1024).
setup:
  files:
    - path: file.txt
      content: "hello\nworld\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail -c 1K file.txt
expect:
  stdout: "hello\nworld\n"
  stderr: ""
  exit_code: 0

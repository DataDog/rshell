description: tail --bytes=N long form is equivalent to tail -c N.
setup:
  files:
    - path: file.txt
      content: "hello\nworld\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail --bytes=6 file.txt
expect:
  stdout: "world\n"
  stderr: ""
  exit_code: 0

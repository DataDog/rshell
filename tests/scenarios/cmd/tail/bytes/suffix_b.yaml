description: tail -c 1b outputs the last 512 bytes (b suffix = 512).
setup:
  files:
    - path: file.txt
      content: "hello\nworld\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail -c 1b file.txt
expect:
  stdout: "hello\nworld\n"
  stderr: ""
  exit_code: 0

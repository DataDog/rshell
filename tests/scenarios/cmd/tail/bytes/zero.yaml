description: tail -c 0 produces no output.
setup:
  files:
    - path: file.txt
      content: "alpha\nbeta\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail -c 0 file.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

description: tail exits 1 and prints an error for an unknown flag.
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail --follow file.txt
expect:
  stdout: ""
  stderr_contains: ["tail:"]
  exit_code: 1

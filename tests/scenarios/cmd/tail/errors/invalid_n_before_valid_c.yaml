description: tail fails on an invalid -n even when a valid -c follows and would win.
setup:
  files:
    - path: file.txt
      content: "hello\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail -n abc -c 2 file.txt
expect:
  stdout: ""
  stderr_contains: ["tail:"]
  exit_code: 1

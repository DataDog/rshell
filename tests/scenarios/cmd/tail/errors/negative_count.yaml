description: tail rejects negative counts (we do not implement elide-head mode).
setup:
  files:
    - path: file.txt
      content: "hello\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail -n -1 file.txt
expect:
  stdout: ""
  stderr_contains: ["tail:"]
  exit_code: 1

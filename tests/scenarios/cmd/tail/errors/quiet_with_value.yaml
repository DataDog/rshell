description: tail --quiet=false is rejected because boolean flags do not take values.
setup:
  files:
    - path: a.txt
      content: "hello\n"
    - path: b.txt
      content: "world\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail --quiet=false a.txt b.txt
expect:
  stdout: ""
  stderr_contains: ["tail:"]
  exit_code: 1

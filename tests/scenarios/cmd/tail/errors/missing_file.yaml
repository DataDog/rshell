description: tail exits 1 and prints an error when a file does not exist.
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail nonexistent.txt
expect:
  stdout: ""
  stderr_contains: ["tail:"]
  exit_code: 1

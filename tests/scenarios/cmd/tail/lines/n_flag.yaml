description: tail -n 3 outputs the last 3 lines of a file.
setup:
  files:
    - path: file.txt
      content: "alpha\nbeta\ngamma\ndelta\nepsilon\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    tail -n 3 file.txt
expect:
  stdout: "gamma\ndelta\nepsilon\n"
  stderr: ""
  exit_code: 0

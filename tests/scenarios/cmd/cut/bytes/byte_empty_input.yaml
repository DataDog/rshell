# Derived from GNU coreutils cut.pl test w
description: Cut -b1 on empty input produces no output.
setup:
  files:
    - path: input.txt
      content: ""
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    cut -b1 input.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

description: Cut on empty input produces no output for byte selection.
setup:
  files:
    - path: input.txt
      content: ""
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    cut -c4 input.txt
expect:
  stdout: ""
  stderr: ""
  exit_code: 0

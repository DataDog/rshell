description: Cut supports multi-character output delimiters.
setup:
  files:
    - path: input.txt
      content: "a:b:c\n"
      chmod: 0644
input:
  allowed_paths: ["$DIR"]
  script: |+
    cut -d: --output-delimiter=_._ -f2,3 input.txt
expect:
  stdout: "b_._c\n"
  stderr: ""
  exit_code: 0

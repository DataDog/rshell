description: cut with consecutive delimiters preserves empty fields.
setup:
  files:
    - path: input.txt
      content: "a::c\n"
input:
  allowed_paths: ["$DIR"]
  script: |+
    cut -d: -f2 input.txt
expect:
  stdout: "\n"
  stderr: ""
  exit_code: 0

description: NR prefixes each printed record
upstream:
  suite: onetrueawk
  id: testdir/p.4
  ref: 3c2e168a8f794ed61c93131b05fb998d79d155df
covers:
  - "NR prefixes each printed record"
  - "uses original rshell fixture data rather than upstream country records"
input:
  program: |
    { print NR ":" $0 }
  stdin: |
    alpha
    beta
    gamma
expect:
  stdout: |
    1:alpha
    2:beta
    3:gamma
  exit_code: 0

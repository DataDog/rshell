description: anchors and dot match exactly one-character records
upstream:
  suite: onetrueawk
  id: testdir/p.16
  ref: 3c2e168a8f794ed61c93131b05fb998d79d155df
covers:
  - "anchors and dot match exactly one-character records"
  - "uses original rshell fixture data rather than upstream country records"
input:
  program: |
    /^.$/ { print "single", $0 }
  stdin: |
    a
    ab
    7

expect:
  stdout: |
    single a
    single 7
  exit_code: 0
